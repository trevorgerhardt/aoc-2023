{
  "name": "aoc-2023",
  "private": true,
  "scripts": {
    "fmt": "bunx prettier . --write",
    "lint": "bunx @biomejs/biome check --apply ./",
    "precommit": "bun run fmt && bun run lint",
    "postinstall": "cp hooks/* .git/hooks/"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
